// // Delete Todo function
// const deleteTodo = (index) => {
//   alert('Delete BTN');
// };

// export default deleteTodo;
// a

//  const filtered = todos.filter((todo) => todo.id !== e.target.parentNode.dataset.id)
//       // localStorage.setItem(localStorageTodos, JSON.stringify(filtered));
//       // e.target.closest('ul>li').remove();

// Map use
// const filtered = todos.filter(
//   (todo) => todo.id !== e.target.parentNode.dataset.id
// );
// localStorage.removeItem(localStorageTodos);
// const newTodos = filtered.map((todo) => {
//   const list = createList(todo.description);
//   return list;
// });
// console.log(newTodos);
// // localStorage.setItem(localStorageTodos, JSON.stringify(newTodos));
// // e.target.closest('ul>li').remove();
// // console.log(filtered);
